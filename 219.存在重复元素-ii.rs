use std::collections::HashMap;


/*
 * @lc app=leetcode.cn id=219 lang=rust
 *
 * [219] 存在重复元素 II
 */
impl Solution {
    pub fn contains_nearby_duplicate(nums: Vec<i32>, k: i32) -> bool {
        let map = HashMap::with_capacity(nums.len());
        nums.iter().for_each(|&n|)
    }
}

